# Xcode project is generated by XcodeGen from project.yml — don't check it in
*.xcodeproj/
*.xcworkspace/

# Xcode user/build state
xcuserdata/
DerivedData/
.build/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/

# macOS
.DS_Store

# Supabase secrets — real config with the project URL + anon key.
# Commit SupabaseConfig.example.plist as a template; never commit the real one.
PokerNight/Config/SupabaseConfig.plist
